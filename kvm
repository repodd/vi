sudo apt-get update
egrep -c '(vmx|svm)'/proc/cpuinfo
kvm-ok
sudo apt install cpu-checker
sudo apt install -y qemu-kvm virt-manager libvirt-daemon-system virtinst libvirt-clients bridge-utils
sudo systemctl start libvirtd
sudo systemctl status libvirtd
dd logged-in user : $ sudo usermod -aG libvirt $USER


$ sudo dpkg -i CiscoPacketTracer822_amd64_signed.deb

if error 
  $ sudo apt-get install -y libgl1-mesa-glx
  $ sudo apt-get install -y libxcb-xinerama0-dev
  $ sudo dpkg -i CiscoPacketTracer822_amd64_signed.deb

run :
  $ packettracer

CISCO packet tarcer installation in ubunu
sudo apt-get update -y
sudo dpkg -i CiscoPacketTracer822_amd64_signed.deb
Click OK and Enetr YES
sudo apt-get update -y
$sudo apt-get install -y libgl1-mesa-glx
sudo apt-get install -y libxcb-xinerama0-dev
After installing the missing packages, install Packet Tracer again.
sudo dpkg -i CiscoPacketTracer822_amd64_signed.deb
$packettracer
